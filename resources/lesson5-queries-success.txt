You can see the response for the request in the box below. It will still return GeoJSON objects, but it only return features whose `Population` is less than 2 million, since you specified it in the query.